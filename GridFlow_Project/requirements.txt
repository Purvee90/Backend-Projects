# ── Data Ingestion Agent ─────────────────────────────────────────────────────
requests>=2.31.0
pandas>=2.1.0
pyarrow>=14.0.0          # Parquet support for pandas & BigQuery upload
google-cloud-bigquery>=3.13.0
google-cloud-bigquery-storage>=2.24.0   # faster BQ reads via Arrow
db-dtypes>=1.2.0         # BigQuery ↔ pandas type bridge
tqdm>=4.66.0             # progress bars during downloads

# ── dbt Transformation Agent ─────────────────────────────────────────────────
dbt-core>=1.8.0,<2.0.0
dbt-bigquery>=1.8.0,<2.0.0

# ── Development / QA ─────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
ruff>=0.1.0
pre-commit>=3.5.0
python-dotenv>=1.0.0     # load .env for local dev
